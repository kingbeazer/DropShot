@page "/verify-result/{Token}"
@rendermode InteractiveServer

<MudProviders />

<DropShot.UI.Components.Pages.VerifyResult Token="@Token" />

@code {
    [Parameter] public string Token { get; set; } = "";
}
